命题：深度优先搜索 用来标记所有 与起点相连通的所有顶点 所需要的时间 与 顶点的度数之和 成正比。
证明：
    1 DFS能够标记 所有与起点s相连通的顶点（且不会去标记其他顶点）；
    事实1：DFS算法只会通过边 来 查找顶点； -> 因此 每个被标记的顶点肯定都是 与s相连通的
        假设：存在有一个 没有被标记、但是却与起点s连通的顶点w；
        由于：1 s是已经被标记了的； 2 s与w是连通的；
        所以，path(s->w)中必然存在这样一条边(比如v-x)，v-x边连接的两个顶点中，一个被标记了，另一个没有被标记。
        由于算法的原理，在标记了v之后，一定会发现x并对其进行标记。 所以假设不成立

    2 由于算法保证了 每个顶点都只会被访问一次， 因此算法有时间上限（检查&标记的耗时 与 所有顶点的度数成正比）

DFS的具体过程：
    这个过程与 图的邻接表数组 有直接关系。
特征：
    1 在算法结束后，所有与起点相连通的顶点都会被标记为true；
    2 分析调用栈时，最终栈中的顶点会被清空。