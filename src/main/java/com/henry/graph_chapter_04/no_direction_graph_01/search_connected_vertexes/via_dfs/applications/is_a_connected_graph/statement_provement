命题：深度优先搜索 “标记 所有 ‘与起点相连通’的所有顶点” 所需要的时间 与 ”顶点的度数“之和 成正比。
证明：
    #1 DFS 能够标记 所有”与起点s相连通“的顶点（且 不会去标记 其他顶点）；
    事实1：DFS算法 只会 通过边 来 查找顶点，因此 每个”能够被标记的顶点“ 肯定都是 与s相连通的；
        假设：存在有一个 ”没有被标记、但却与起点s连通“的顶点w；
        由于：① s 是 已经被标记了的； ② s 与w是相连通的；
        所以，path(s->w)中 必然存在 这样一条边(比如v-x)，在 v-x边 所连接的两个顶点 中，一个 被标记了，另一个 没有被标记。
        由于 算法的原理，在 标记了v 之后，一定会发现x 并 对其进行标记。
        所以 假设不会成立。证毕~

    2 由于 算法 保证了 每个顶点都 只会被访问一次，因此算法有 时间上限（检查&标记的耗时 与 所有顶点的度数 成正比）

DFS的具体过程：
    这个过程 与 图的邻接表数组 有直接关系。
特征：
    1 在 算法结束 后，所有 与起点相连通的顶点 都会 被标记为true；
    2 分析 调用栈 时，最终 栈中的顶点 会被清空。