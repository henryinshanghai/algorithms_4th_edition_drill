搜索过程：
共性
    1 取出 某一个节点v 并 标记它；
    2 把 v的所有 相邻且没有被标记的节点 添加到 数据结构中。
    重复，直到 数据结构 为空
差异
    从数据结构中 取出 下一个要处理的节点 的规则不一样 - 这导致了 遍历边的规则 的不同

DFS 获取到的是 最晚添加到数据结构中 的节点 - 这导致 DFS处理边时 会不断纵深
BFS 获取到的是 最早添加到数据结构中 的节点 - 这导致 BFS处理边时 会不断扩展

