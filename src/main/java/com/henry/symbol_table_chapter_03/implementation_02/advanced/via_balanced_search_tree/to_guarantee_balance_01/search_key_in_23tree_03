== 在2-3树中 查找指定的键：==

算法步骤：
    #1 比较 传入的key 与 根结点中的key；
    #2 如果 传入的key 与 根结点中的key（任意一个）相等，则：查找命中(successful hit)；
    #3 如果不相等，则：根据比较结果，在 相应的子树 中继续递归查找；
    #4 当结点为 空结点/空链接 时，查找未命中(failed hit)。

评论：这个过程 与 在BST中查找的过程 完全一致。