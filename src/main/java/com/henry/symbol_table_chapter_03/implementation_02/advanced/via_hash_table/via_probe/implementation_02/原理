这段符号表的实现 把键与值 分别保存在 两个数组中（这种做法与 OrderedArraySymbolTable 相同）。
并且使用 空(null) 来 表示一簇键的结束。

如果一个 新键的散列值的位置 是一个空元素，则：这个新元素就会被保存在这里；
如果不是，则：顺序查找 散列表中的下一个空元素位置 来 保存此新元素。

查找键的算法：
#1 得到 键的散列值，然后 从散列值位置出发，开始 顺序查找；
#2 如果找到，则命中； 如果没有找到，则未命中。

