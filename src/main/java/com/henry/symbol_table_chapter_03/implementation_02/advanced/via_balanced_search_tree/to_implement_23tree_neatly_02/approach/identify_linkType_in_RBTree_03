🐖 二叉树中的每一个结点 都有一个 指向它自己的链接（在BST中，并没有显式地表示它）
在红黑树中，我们需要表示出 链接的颜色。
手段：在Node类型中，引入一个 表示 ”指向自己的链接的颜色“ 的 布尔类型的实例变量。
约定：
    #1 如果 指向它的链接 是红色的，则：该变量的值为true。黑色则为false。
    #2 空链接的颜色 为黑色。
术语统一：
    结点的颜色：”指向该结点的“ 链接的颜色。
辅助方法：isRed() 用于判断 当前结点 和 它的父节点 之间的 链接的颜色。
