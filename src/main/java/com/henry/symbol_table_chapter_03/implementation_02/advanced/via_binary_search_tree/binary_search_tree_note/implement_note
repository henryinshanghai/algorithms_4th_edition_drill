-- 二叉查找树中的结点(Node)的数据表示 --
结点中 包含 如下字段：
    1 键key;
    2 值value；
    3 结点的左链接(leftSubNode)；
    4 结点的右链接(rightSubNode)；
    5 结点计数器 nodeAmountInTree - 用于 记录 以当前节点作为根节点的子树 中 所包含的 所有结点的数量（一般 在图形表示中，写在 结点的上方）
        应用：用于支持 实现rank(passedKey) 与 selectOut(passedRank) 这样的API

        对于 根节点为x的二叉查找树，存在 如下恒等式：
            nodeAmountInTree(x) = nodeAmountInTree(x.left) + nodeAmountInTree(x.right) + 1

-- #1 查找操作 --
二叉查找树（键的）数值约束: 左子树 < 根节点 < 右子树

执行过程👇
在 二叉查找树 中 进行查找时，会需要 传入一个key（passedKey）：
    如果 BST中 存在有 相同的key，则：查找命中；
    否则，查找 未命中（get()方法 会返回null）

具体实现👇
根据 二叉树的递归结构，get()方法 可以有 如下递归实现：
#1 如果 树是空的，则：查找 未命中；
#2 如果 传入的key 与 根节点的key 相同，则：查找命中；
#3 否则，就需要 递归地 在合适的子树（👇）中 继续查找
    合适的子树：如果 传入的key 比 根节点key 小的话，就 在左子树中查找。否则，在右子树中查找
