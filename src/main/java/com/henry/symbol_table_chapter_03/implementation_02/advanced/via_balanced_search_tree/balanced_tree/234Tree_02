#1 234树的节点类型 可以通过 增减键值 来 相互转换；
#2 三键四叉结点 增加键值后，变成 大肚子结点，必须分裂；
#3 分裂后，推送 大肚子节点中的某一个键值 给父节点，这样父节点 就能够携带 由分裂产生的 多出来的子节点；
#4 满树的定义：树中的每一个结点 都有最多的子节点，叶子节点除外；
#5 满树的特征：叶子结点 全部在同一层中；
#6 234树的特征：节点的灵活性 使得 使用234树 来 构造和维持满树 变得非常容易；
#7 234树的添加键值操作 全部发生在子节点上；
#8 叶子节点向上推送键值，使得 234树向着根部方向生长。