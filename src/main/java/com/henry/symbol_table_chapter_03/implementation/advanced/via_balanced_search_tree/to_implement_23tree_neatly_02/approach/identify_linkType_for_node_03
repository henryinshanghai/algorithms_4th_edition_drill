🐖 二叉树中的每一个结点都有一个指向它自己的链接（在BST中，并没有显式地表示它）
在红黑树中，我们需要表示出链接的颜色。
手段：在Node类型中，引入一个表示 ”指向自己的链接的颜色“的布尔类型的实例变量。
约定：
    #1 如果指向它的链接是红色的，则该变量的值为true。黑色则为false。
    #2 空链接的颜色为黑色。
术语统一：
    结点的颜色：指向该结点的链接的颜色。
辅助方法：isRed() 用于判断当前结点和它的父节点之间的链接的颜色。
