为了分析算法，我们将重点放在不同算法 访问任意数组元素的总次数上。
这相当于 隐式地猜测 各个算法 在一台特定的计算机上的运行时长 = “访问任意元素的总次数” * 某一个常量的范围之内。
这个猜想 基于代码，用实验来验证它其实不困难。
事后回顾：这样的猜想 是 “对算法进行比较” 这个目标的一个很好的开始。

union-find的成本模型：
在研究 实现union-find的API 的各种算法时，我们统计的是 数组的访问次数（无论读写）；

