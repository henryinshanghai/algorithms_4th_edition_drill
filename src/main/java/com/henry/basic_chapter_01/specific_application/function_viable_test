为了测试API的可用性并方便开发，我们在main()方法中包含了一个用例 用于解决动态连通性问题。
它会：
#1 从输入中读取 N的值 以及 一系列的整数对，并且 对每一个整数对调用 isConnectedBetween() 来 判断两个整数是否已经连通；
#2 如果 已经连通，则：跳过当前整数对，处理下一个整数对；否则，调用 unionToSameGroup()方法 来 使他们连通。

测试数据：
tinyUF.txt 10个触点、11条连接；
mediumUF.txt 625个触点、900条连接；
largeUF.txt 100万个触点、200万条连接；

目标：在可以接受的时间范围内 来 处理与largeUF.txt规模相当的输入。

