小型子数组

在所有的递归算法中，我们都可以通过 对小型子数组进行特殊处理 来 提高效率。
这里使用的是 如下的插入排序（对 “前d个字符均相同”的字符串 执行插入排序），
它能够跳过 已知相等的字符。

这项修改带来的改进会很明显，尽管
它在三向字符串排序中的重要性 远不如 它在高位优先的字符串排序中的重要性 高。

===
有限的字母表

为了处理特殊的字母表，可以
为所有方法添加一个 Alphabet类型的参数 alpha，
并 在charAt()方法中，将s.charAt(d) 替换为 alpha.toIndex(s.charAt(d)).
但是，在此处 这么做并不能得到什么收益，相反
添加这段代码 可能会 大幅降低 算法的运行速度 - 因为它在内循环之内。
