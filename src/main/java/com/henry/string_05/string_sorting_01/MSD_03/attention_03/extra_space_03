额外使用的空间

为了 进行切分，高位优先算法 使用了 两个辅助数组：
#1 一个用于 将数据分类的临时数组 aux[];
#2 一个用于 统计键出现频率的数组 keyToItsSizeArr[];

aux[]的大小 为N，并且 可以 在 递归方法sort() 外 创建。
如果 牺牲 稳定性，还可以 去掉aux[]数组（参考 练习5.1.17）,
它 不是 在实际使用时 所需要关注的内容。

而 keyToItsSizeArr[] 所需要的空间 才是 主要问题，因为
它 没办法 在递归方法sort()的外面 创建。