在把长字符串排序时，使用 高位优先的字符串排序算法的最大挑战是：
处理数组中的非随机因素。

一般来说，某些键 可能存在 较长的公共部分，或者 部分键的取值范围 有限。
比如，在 处理学生信息的应用程序中，数据的键 可能是毕业年份（4个字节，但是只有4种可能的值），
州名（可能需要10个字节，但只有50种可能的值），性别（1个字节，2种值），以及
学生的姓名（和随机字符串最接近，但有可能很长，字母出现频率的分布并不均匀，且
当这栏长度固定时，字符串的末尾会被添加许多空格）。

这些限制，使得高位优先的字符串排序算法 会产生 许多空字符串。
下面我们将 学习一种 能够漂亮地 解决这个问题的算法。