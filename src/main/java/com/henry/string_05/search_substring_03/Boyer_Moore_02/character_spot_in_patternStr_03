===
起点

为了能够实现 启发式地 处理“不匹配的字符”，
我们使用 数组right[] 来 记录字母表中的每个字符 出现在模式中的 最靠右的地方
（如果 字符在模式中不存在，则表示为-1）。
这个值揭示了 如果#1 该字符出现在文本中; #2 且在查找时造成了一次匹配失败的话，定指针（anchor）应该向右跳跃多远。

为了把right[]数组初始化，首先 把所有元素的值都设为-1，
然后对于[0, M-1]的j，把 right[pat.charAt(j)]设为j，
如图，展示了 对模式 N E E D L E的处理。