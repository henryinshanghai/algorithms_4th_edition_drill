===
起点

为了 能够实现 启发式地 处理“不匹配的字符”，
我们使用 数组right[] 来 记录 字母表中的每个字符 出现在 模式字符串中的 最靠右的 位置
（如果 字符 在模式中 不存在，则 表示为-1）。
这个值 揭示了 如果① 该字符 出现在 文本中; ② 且 在查找时 造成了一次 匹配失败 的话，定指针（anchor）应该 向右跳跃多远。

为了 把right[]数组 初始化，首先 把 所有元素的值 都设为-1，
然后 对于[0, M-1]的j，把 right[pat.charAt(j)] 设为j，
如图，展示了 对模式 N E E D L E的处理。