===
游戏规则

现代计算机系统中，处理的所有类型的数据 都有一个共同点👉 它们最终都是使用二进制来表示的。
所以 我们可以把任何数据 看作 一串比特（或者字节-8位比特）的序列。

简单起见，本节中使用“比特流”这个术语 来 表示比特的序列，
使用 “字节流”这个术语 来 表示 “可以看作固定大小的字节序列” 的比特序列。

比特流 或者 字节流
可以是 保存在计算机中的文件，
也可以是 互联网上传输的一条消息。
