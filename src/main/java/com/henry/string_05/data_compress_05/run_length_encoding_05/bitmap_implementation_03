==
实现

由刚才给出的非正式描述 可以立即得到 右边框注中的compress() 和 expand()方法。

和以前一样，expand()方法的实现 相对比较简单👇
#1 读取一个游程的长度；
#2 把当前比特 按照长度打印 并复制；
#3 转换当前比特，然后继续，直到输入结束。

compress()方法也很简单。
对于输入，它执行了以下操作👇
#1 读取一个比特；
#2 如果 它和上一个比特不同，则：写入当前的计数值，并把计数器归零；
#3 如果 它和上一个比特相同，且 计数器已经达到最大值，则：
    ① 写入计数值；② 再写入一个 0计数值；③ 然后把计数器归零；
#4 增加计数器的值。

当输入流结束是，写入计数值（最后一个游程的长度）并结束。
