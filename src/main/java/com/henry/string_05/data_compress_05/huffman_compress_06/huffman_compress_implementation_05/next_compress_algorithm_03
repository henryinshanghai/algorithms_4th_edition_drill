除了 霍夫曼压缩算法，另一种 值得一提的选择是 20世纪70年代末至80年代初，
由 A.J.Ziv 和 T.Welch发明的一种算法。

它的应用 也十分广泛，因为 它的实现 简单，而且 也适用于 多种类型的文件。

这种算法的基本思想 和 霍夫曼算法的基本思想 相反。
霍夫曼算法 会 为 “输入中的定长模式” 产生一张 变长的编码编译表，
而此方法 会 为 “输入中的变长模式” 产生一张 定长的编码编译表。

这种方法的 另一种 令人惊讶的特性 在于，和 霍夫曼编码 不同 - 在 输出 中，并不需要 附上 这张编码表Σ(っ °Д °;)っ