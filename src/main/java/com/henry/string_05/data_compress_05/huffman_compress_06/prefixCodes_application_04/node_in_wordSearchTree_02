==
单词查找树的结点

我们 首先遇到的是 如下框注的Node类。
它 与 我们曾经 “用来构造二叉树 和 单词查找树”的嵌套类 类似：
    每个Node对象中，都含有 “指向其他Node对象”的left 和 right引用，
    这 定义了 单词查找树的结构。

每个Node对象中，还包含有：
① 一个实例变量freq - 构造函数 会使用到 它。
② 另一个实例变量ch 用于表示 “叶子节点中，需要被编码的 字符”。

代码类
《单词查找树中的 结点表示》