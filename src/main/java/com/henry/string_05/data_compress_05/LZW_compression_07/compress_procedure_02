==
压缩数据的算法👇 相当于 在 动态地构建 符号表
为了 压缩数据，只要 输入 还没有结束，就会 不断进行 如下操作👇
#1 对于 “未处理（还没有被输出）的输入”，找出 其存在于符号表中的 “最长前缀字符串”s；
#2 输出 s的8位值（编码）；
#3 继续扫描 原始输入中 “s之后的 下一个字符c”；
#4 在 符号表 中，把 s+c（连接s与c）的字符串 绑定到 “ASCII码表中的下一个码值”。

在 后面的几步 中，我们需要 继续查看 “输入中的下一个字符”，才能够 构造出“字典中的条目”。
所以 我们 把 这个字符c 叫做 “前瞻字符（lookahead）”。

现在，当 用尽了 所有可用的码值（也就是 把 FF 赋予给了 某个子字符串）之后，
我们 暂时只能 先停止 向符号表中 添加 新的条目 ———— 稍后我们会讨论其他的策略。