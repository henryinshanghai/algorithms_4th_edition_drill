==
压缩数据的算法👇
为了压缩数据，只要输入还没有结束，就会 不断进行如下操作👇
#1 对于“未处理（还没有被输出）的输入”，找出 其存在于符号表中的 “最长前缀字符串”s；
#2 输出s的8位值（编码）；
#3 继续扫描 原始输入中“s之后的下一个字符”c；
#4 在符号表中，把s+c（连接s与c）的字符串 绑定到 “ASCII码表中的下一个码值”。

在后面的几步中，我们需要 继续查看“输入中的下一个字符”，才能够 构造出“字典中的条目”。
所以 我们把这个字符c叫做“前瞻字符（lookahead）”。

现在，当用尽了所有可用的码值（也就是把FF赋予给了某个子字符串）之后，
我们暂时只能 先停止向符号表中添加新的条目 ———— 稍后我们会讨论其他的策略。