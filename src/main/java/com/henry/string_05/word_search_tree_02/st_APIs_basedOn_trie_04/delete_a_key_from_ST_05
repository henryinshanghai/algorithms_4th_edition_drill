从一棵单词查找树中删去一个键值对
第一步：找到 键所对应的结点，并把结点的值设置为null;
    如果此结点 存在有 一个非空的链接 指向某一个子节点，则：删除操作结束；
    如果此节点 所有的链接都是空，则：需要从数据结构中，物理删除掉这个结点??? - 为了避免递归时，扫描多余的结点？
        如果物理删除此节点，使得它的父节点的 所有链接也都变成空，则：需要继续删除它的父结点...

如下所示，按照标准的递归流程，这项操作所需要的代码很少：
在递归删除了某个结点x之后，如果该结点的值 和 所有的链接都是空，则：返回null。

