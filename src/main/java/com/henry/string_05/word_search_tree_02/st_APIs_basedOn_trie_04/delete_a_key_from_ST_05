从 一棵单词查找树 中 删去一个键值对

第一步：传入的字符串 在trie树中 是否存在 有效的key；
第二步：找到 键路径的最后一个结点，并 把 结点的值 设置为null 来 移除 键值对的关联;
第三步：对于 路径中的其他结点，如果 结点 是 冗余结点，则：对其进行 物理删除；
    如果 此结点的value 不为null，则：保留 当前结点；
    如果 此结点 所有的链接中 存在有 不为null的子链接，则：保留 当前结点；
    否则，说明 此结点 仅在当前路径中存在，在 删除key 之后，此结点 已经冗余，可以 直接物理删除 此结点。


如下所示，按照 标准的递归流程，这项操作 所需要的代码 很少：
在 递归删除了 某个结点x 之后，如果 该结点的值 和 所有的链接 都是空，则：返回null。