通配符匹配

我们可以使用 一个类似的过程 来 实现keysThatMatch(),
但需要 为 collect()方法 添加一个参数 来 指定“用于匹配的模式”。

如果 模式中 含有 通配字符，就需要 使用 递归调用 来 处理 所有的链接，
否则 就只需要 处理 模式字符串中 指定字符 所对应的链接 即可。

你可能 还会注意到，这里 不需要考虑 长度超过 模式字符串 的键。

代码：
参考 TrieSTWebsite