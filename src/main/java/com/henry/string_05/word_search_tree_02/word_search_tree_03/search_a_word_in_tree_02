目标：在 单词查找树 中，查找 指定的字符串键（key）所对应的值（value）
特征：基于 被查找的字符串 中的字符；
原理：单词查找树 中，每个结点 都拥有 用于表示 下一个可能出现的 所有字符选项 的链接。
算法：
    #1 从 根节点 开始，查询路径 首先经过的是 键的首字母 所对应的链接；
    #2 查询路径 再沿着 第二个字符所对应的链接，向下前进；
    #3 so on and so forth, 直到到达 键的最后一个字母 所指向的结点(命中的情况) 或者 遇到了 一条空链接(未命中的情况)
        如果 键的最后一个字符 所对应的结点，其中的值 不是空，说明 这是一次 命中的查找，则：结点所关联的值 就是 key所对应的值；
        如果 键的最后一个字符 所对应的结点，其中的值 为空，说明 这是一个 未命中的查找，则：符号表(trie树)中 不存在 被查找的键；
        如果 查询结束于 一个空链接，说明 单词查找树中 不存在 某个字符所对应的链接，则：也是一次 未命中的查找。

在 所有的情况 中，执行查询的方式 就是：
    在 单词查找树 中，从 根节点 开始，逐个检查 查询路径上的所有结点。