# 589

题设与要求：
//给定一个 N 叉树，返回其节点值的前序遍历。

分析难点 & 提出解决手段
难点：如何按照前序遍历的规则来访问树中的节点？
手段1：递归；
手段2：循环 + 用于辅助的数据结构(双端队列)

---
手段2SOP：
    1 准备一个deque与list；
    2 把根节点入栈deque； aka  把deque作为一个栈来使用
    3 准备一个循环：
        3-1 出栈栈顶元素，并把栈顶元素添加到列表中（尾部添加）；
        3-2 从右往左遍历当前节点的子节点，并把所有的子节点入栈————这样从栈中弹出元素时，得到的就是左 -> 右的顺序

为什么直接使用一个队列临时存储节点得到的结果是错的？
因为这样会逐层打印节点，得到的是层序遍历的结果。而不是前序遍历

expr：
    1 充分利用双端队列两端都可以操作的特性；
    2 过早的优化会让你的关注点失去焦点，这就是为什么它如此臭名昭著

DONE yet no pride~ just tired
练会打字吧


