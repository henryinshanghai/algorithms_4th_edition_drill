# 49 题设与要求
//给定一个字符串数组，将字母异位词组合在一起。字母异位词指字母相同，但排列不同的字符串。

示例：
// 输入: ["eat", "tea", "tan", "ate", "nat", "bat"]
//输出:
//[
//  ["ate","eat","tea"],
//  ["nat","tan"],
//  ["bat"]
//]

分析难点:要把所有互为字母异位词的单词组合到一起,怎么组合呢?
通过对字符计数能够判断两个字符串是否互为字母异位词,但是多个单词之间如何比较呢?
特征:互为字母异位词的单词,他们的排序结果(字母自然顺序)必然是同一个字符串;
手段:
    1 获取到当前字符串;
    2 对当前字符串中的字符进行排序;
        2-1 先把字符串转化为字符数组;
        2-2 对字符数组进行排序[java中不能直接对字符串中的字符进行排序吗? 是的]
        2-3 把排序后的数组转化成为新的字符串;
    3 把排序后得到的字符串添加到一个map对象中;
        3-1 添加前先判断key是否已经存在,
            如果已经存在,则把当前字符串s追加到列表中;
            如果尚未存在,则为当前字符串新创建一个新列表,并把当前字符串添加到列表中;

代码:

