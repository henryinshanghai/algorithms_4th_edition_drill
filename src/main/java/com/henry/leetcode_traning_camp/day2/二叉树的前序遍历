手段1：递归方式
// 递归方式
        // 1 递归终结条件
        if (root == null) return res;

        // 2 本级递归要做的事情：根 - 左 - 右
        res.add(root.value);
        preorderTraversal(root.left);
        preorderTraversal(root.right);

        return res;

---
手段2：迭代方式
前序遍历： 根 - 左 - 右
public preOrder(Node root){
    List<Integer> res = new ArrayList<>();

    Stack<Node> stack = new Stack<>(); // Java中是有Stack这种数据类型的
    stack.push(root);

    while(!stack.isEmpty()){
        root = stack.pop();
        res.add(root.value);

        if(root.right != null){
            stack.push(root.right);
        }

        if(root.left != null){
            stack.push(root.left);
        }
    }

    return res;
}
